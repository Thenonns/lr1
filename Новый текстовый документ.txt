#include <iostream>
#include <string>
#include <fstream>
using namespace std;
int main() {
	setlocale(LC_ALL, "Russian");
	string a;
	ifstream fin;
	ifstream path("th.txt");
	
	fin.open(path);
	if (!fin.is_open()) {
		cout << "Ошибка открытия файла" << endl;
	}
	else {
		cout << "Файл открыт" << endl;
		char sm;
		while (fin.get(sm)) {
			cout << sm;
			if (a.find(',' || ':' || '.' || ';') != string::npos)
			{
				string temporery;

				cout << temporery;
			}
			else return 1;

		}
	}
	path.close();
}